### to do:
-change correlation page to have a correlation heat map instead of choosing variables to correlate.
-see if you can clean general gun violence data set to make use of variables such as age range and gender.